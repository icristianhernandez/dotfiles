-- IDE-like breadcrumbs, out of the box.
return {
    -- "Bekaboo/dropbar.nvim",
    -- lazy = false,
    -- keys = {
    --     -- pick mode: require('dropbar.api').pick()
    --     { "<leader>ue", ":lua require('dropbar.api').pick()<CR>", desc = "Pick Mode Dropbar" },
    -- },
}
